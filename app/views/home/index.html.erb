<div class="row">
  <div class="col-md-3 col-12 d-note d-md-block">
    <div class="card p-2">
      <%= image_tag current_user.avatar.url, class: "card-img-top rounded-circle" %>
      <div class="card-body">
        <h4 class="card-title"><%= "@#{@user.name}" %></h4>
        <% if @user.admin? %>
          <p class="card-text">
            <span class="text-secondary">管理者だよ！</span>
          </p>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-md-9 col-12">
    <%= link_to "新規登録", new_caption_path, class: 'btn btn-primary btn-lg btn-block' %>
    <% if @user.admin? %>
      <%= link_to 'Exel出力', captions_path(format: :xlsx), data: {confirm: "Exel出力しますか？少し時間がかかりますよ？"}, class: 'btn btn-success btn-lg btn-block' %>
    <% end %>
    <%= render partial: "caption", collection: @captions %>
  </div>
  <%= paginate @captions %>
</div>
